import tailwindcss from '@tailwindcss/vite';
import react from '@vitejs/plugin-react';
import path from 'path';
import {defineConfig} from 'vite';

export default defineConfig(() => {
  return {
    plugins: [react(), tailwindcss()],
    resolve: {
      alias: {
        '@': path.resolve(__dirname, '.'),
      },
    },
    build: {
      rollupOptions: {
        input: {
          main: path.resolve(__dirname, 'index.html'),
          bmi: path.resolve(__dirname, 'bmi-calculator.html'),
          calorie: path.resolve(__dirname, 'calorie-calculator.html'),
          water: path.resolve(__dirname, 'water-intake-calculator.html'),
          sleep: path.resolve(__dirname, 'sleep-cycle-calculator.html'),
          bmr: path.resolve(__dirname, 'bmr-calculator.html'),
        },
      },
    },
    server: {
      port: 3000,
      host: '0.0.0.0',
      hmr: process.env.DISABLE_HMR !== 'true',
      watch: process.env.DISABLE_HMR === 'true' ? null : {},
    },
    preview: {
      port: 3000,
      host: '0.0.0.0',
    },
  };
});
